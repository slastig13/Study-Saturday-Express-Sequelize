const router = require('express').Router()
const Students = require('../db/models/student')

// router.get('/students', (req, res, next) => {
//   try {
//     let students = Students.findAll()
//     console'log
//     res.json(students)
//   } catch (error) {
//     next(error)
//   }
// })

module.exports = router
